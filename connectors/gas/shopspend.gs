// Scaffold placeholder for the shopSpend silo (connectors/gas/shopspend.gs).
// Implementation lands in the GREEN sub-phase of step 1 — this file exists
// only so test_code.js's load('shopspend.gs') does not fail with ENOENT
// during RED.
