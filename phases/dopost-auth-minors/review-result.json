{
  "verdict": "revise",
  "issues": [
    {
      "severity": "important",
      "file": "connectors/shopspend/ingest.py",
      "summary": "Graceful degradation covers only an UNSET GAS_READ_TOKEN, not a stale/rejected one. post_pull (ingest.py:139-180) resolves the token and, if falsy, drops weeks_verified_empty and keeps pulling. If the token is present but no longer matches API_READ_TOKEN — the exact failure this project has already hit twice (see memory: gas-read-token-name-mismatch, token-mismatch-rotate-typed-not-debug-clipboard, and the rotation path in docs/api.md) — GAS answers {result:'error', message:'unauthorized'} with NO `code` field (Code.gs:145), so _send() cannot distinguish it from any other rejection and raises IngestFailed. runner.py:402 catches it and returns 1 AFTER earlier chunks have already been written to the ShopSpend tab, and the final pull-marker POST never runs — so the pull lands partial data with no ShopSpendPulls row, and the ShopSpend Report loses the banner/diagnostics for that pull. This contradicts what docs/api.md:143-147 and PRD-9 promise ('degrades non-destructively ... keeps pulling when the token is unresolvable'): an invalid token is at least as likely as an absent one and is the only new failure mode this phase introduces. Nothing in test_ingest.py exercises a token-rejected response (the 5 new degradation tests all monkeypatch get_credential to return None). Suggested fix: have GAS return code:'UNAUTHORIZED' on the doPost gate and have _send/post_pull treat that code on a weeks_verified_empty chunk as the same degradation — retry the chunk once without weeks_verified_empty + warn — instead of aborting the pull."
    },
    {
      "severity": "minor",
      "file": "docs/PRD.md",
      "summary": "PRD-9 (line 25) is still status `planned` even though the phase covering it has completed all 5 steps. PRD.md's own rule directly under the table says to flip planned -> built when the covering phase completes, and step 4 was the docs-closeout step declaring covers:[PRD-9] — so the closeout step left its own PRD row stale."
    },
    {
      "severity": "minor",
      "file": "connectors/shopspend/ingest.py",
      "summary": "The degradation path leaves the pull marker internally inconsistent in one remaining place. It appends the warning to pull['warnings'] / pull['warnings_count'] and rewrites diagnostics_json.harness (weeks_verified_empty cleared + verified_empty_dropped flag), but diagnostics_json also carries its own top-level 'warnings' array (populated from diag.warnings at runner.py:218), which is left without the degradation message. The two warning lists in the same marker now disagree. The Sheet-facing banner reads the warnings column (shopspend.gs:322 -> pull[8]), so the user-visible surface is correct; this is a diagnostics-blob consistency wart, not a data-correctness bug."
    },
    {
      "severity": "minor",
      "file": "phases/index.json",
      "summary": "The phase is registered as {\"dir\":\"dopost-auth-minors\",\"status\":\"error\",\"failed_at\":\"2026-08-05T15:00:37+0900\"} while phases/dopost-auth-minors/index.json shows all 5 steps `completed` (steps started 15:20+, i.e. after that failed_at stamp). Stale harness state from an aborted earlier run; leaves the phase index claiming a failure that the step records contradict."
    },
    {
      "severity": "minor",
      "file": "connectors/gas/Code.gs",
      "summary": "No privilege separation between read and write: the new doPost gate (Code.gs:143-146) reuses checkReadToken_/API_READ_TOKEN, so any holder of the read-only doGet credential is also authorized to POST weeks_verified_empty and bypass the blast-radius breaker (a destructive tombstone path). This is what PRD-9 specifies and it is strictly better than the previous unauthenticated write path, so it is not a regression — but a distinct API_WRITE_TOKEN would keep a leaked read credential from carrying destructive capability. Jake's call, worth a follow-up item rather than a rework."
    }
  ],
  "notes": {
    "tdd_audit": "Clean. Steps 0-3 are tdd:true and every one carries both red and green tdd_evidence in index.json with a real test_cmd; tdd_state is red_done on all four (the only value execute.py emits). Step 4 is the only tdd:false step and its commit (1460f69) touches TODO.md, docs/api.md and index.json only — zero lines of connectors/ or scripts/, so no logic used the scaffolding escape hatch. New behaviour is genuinely covered: 15 GAS assertions across no-token / wrong-token / correct-token / empty-array-present / field-absent-shopspend / field-absent-suppliers / property-unset fail-closed / auth-precedes-validation, plus the 01-53 bound on both the array and per-row week_label paths; and 11 pytest cases including token-attached-only-to-verified-empty-chunks, marker warning + diagnostics rewrite, truncated-diagnostics no-raise, raw-list warnings, and test_token_value_never_printed. Only the token-rejected path (issue 1) is untested.",
    "boundary_audit": "token field: poster sends payload['token'] (ingest.py:227), GAS reads body.token (Code.gs:144) — names and casing agree, and docs/api.md correctly states body-not-query-string (doPost never reads e.parameter.token). GAS_READ_TOKEN (local .env) -> API_READ_TOKEN (Script Property) is the deliberate documented mismatch, not drift. bc.get_credential(name)->str|None matches the callsite. pull['warnings'] / ['warnings_count'] / ['diagnostics_json'] are all pre-existing keys built by runner._build_pull (runner.py:241-242) — no new marker field invented, so no ShopSpendPulls header migration is required (which matters given ensureSheet only writes headers when the tab is missing). No orphan endpoint or callsite. timingSafeEqual_ verified correct on all four branches (equal/unequal length, empty strings are unreachable behind the !stored / !params.token guards).",
    "tests_run": "node connectors/gas/test_code.js -> 830 passed, 0 failed. python -m pytest connectors/shopspend -q -> 137 passed. Both run this session on HEAD 65f2de6."
  }
}
