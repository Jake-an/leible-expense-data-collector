{
  "red_valid": true,
  "reason": "Tests fail because compute_weeks_complete() function is missing from shopspend.runner and main() isn't computing/passing weeks_complete and weeks_verified_empty to post_pull — pure missing implementation, no test code issues."
}
