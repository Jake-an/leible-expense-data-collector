{
  "verdict": "revise",
  "reviewed_range": "b5d653a..HEAD (phase orderapp-pulls; main...HEAD spans 297 unrelated commits)",
  "test_run": "node connectors/gas/test_code.js -> 1043 passed, 0 failed",
  "issues": [
    {
      "severity": "important",
      "file": "connectors/gas/orderapp.gs",
      "line": 457,
      "summary": "greenBeanFetchAllRows_ has an unbounded `while (true)` paging loop with no page cap and no guard that the offset advances. If a response carries paging.truncated:true + rowsIncluded:true with paging.returned === 0 (or a renamed/absent `returned`, which makes `offset += paging.returned` NaN), the identical UrlFetch is re-issued forever until the GAS 6-minute limit kills the trigger — hundreds of wasted fetch-quota calls, zero ingest, every Tuesday. The shopify.gs puller deleted in this same phase carried exactly this guard (SHOPIFY_MAX_PAGES = 50); it was dropped and not replaced. Tests cover the happy path, the size-guard abort and rowNumber dedup, but never a non-advancing page."
    },
    {
      "severity": "important",
      "file": "connectors/gas/orderapp.gs",
      "line": 302,
      "summary": "shopifyWeeklyPull_impl_ reads body.meta.weekStart and Number(body.summary.grossSales) with no shape validation — orderAppClassifyResponse_ only checks parsed.ok === true. Three failure modes, all money-affecting: (1) a 200 {ok:true} body missing `summary` throws a TypeError straight out of a scheduled trigger, contradicting orderAppFetch_'s documented 'never left to propagate' contract; (2) grossSales absent/null/non-numeric yields NaN or 0 written into the Summary `total` column while orderAppRunSuccess_ still stamps the heartbeat and raises no alert — and because NaN !== NaN, upsertRows_ then rewrites that row every single week while doGet serves the corrupt figure to the weekly report; (3) meta.weekStart is never checked against the requested weeks[i].label, so an API that ignores or clamps the `week` param writes one week four times (3 in-batch duplicatesSkipped) and still reports weeksFetched:4 with no apiFailed. Contrast greenBeanPull, which does surface upstream warnings/flags. No test covers a 200-ok body with an unexpected shape."
    },
    {
      "severity": "minor",
      "file": "connectors/gas/orderapp.gs",
      "line": 387,
      "summary": "greenBeanInvoices_ accumulates `groups[k].total += line.totalCostIncGst` with no Number() coercion, unlike every other money read in the codebase (Number(body.summary.grossSales), Number(row.amount), Number(existingRow[2])). A string-typed totalCostIncGst makes the accumulator a string: 0 + '50' -> '050', then '050' + '25' -> '05025', and Math.round('05025' * 100) / 100 emits 5025 — a 100x overstated invoice, silently ingested into Suppliers and Summary with no flag and no alert. undefined yields a NaN total. The API's own `flags` mechanism exists precisely because 06_Stock_Intake cells can be non-numeric, so a numeric guarantee is not safe to assume here."
    },
    {
      "severity": "minor",
      "file": "connectors/gas/orderapp.gs",
      "line": 662,
      "summary": "The resummarize loop discards weeklySummarize's return value and then unconditionally shrinks the queue to `remaining`, so a week that returned {refused:...} or an early `return;` is dropped from the queue as if it had been summarized, with nothing logged. Today every refusal path is excluded by construction (the incomplete-week guard in addAffectedWeek, ensureSheet having already created Suppliers, and withScriptLock_'s reentrancy depth), so this is latent rather than live — but the crash-safety design deliberately makes the queue the sole record of pending weeks, and this is the one path that can silently empty it."
    },
    {
      "severity": "minor",
      "file": "connectors/gas/staleness.gs",
      "line": 30,
      "summary": "'coffee_order_app' was removed from STALENESS_SOURCES on the stated grounds that it 'never stamps a heartbeat', but doPost stamps stalenessStampHeartbeat_(body.source) on every successful ingest (Code.gs:184) and kind:'revenue' from coffee_order_app is still explicitly sanctioned (docs/ingest-contract.md, tested at test_code.js:2699). Only the suppliers-kind shape was rejected. So the moment the Order app's wholesale-revenue POSTer ships, it stamps LAST_INGEST_coffee_order_app and sits unwatched, and testEveryHeartbeatSourceIsWatched cannot catch it because STAMPS_HEARTBEAT is a hand-maintained literal. Either re-add it, or make the exclusion conditional on there being no revenue writer."
    }
  ],
  "verified_clean": [
    "TDD discipline: all 7 steps are tdd:true with both red and green tdd_evidence in index.json — no tdd:false escape hatch, no logic smuggled into scaffolding. Suite re-run this review: 1043 passed, 0 failed.",
    "Summary row shape matches SUMMARY_HEADERS/SUMMARY_KEY_COLS exactly (week_start|week_end|supplier|location|total|summarized_at|department|kind); snapshot column indices 0/2/3/5 match SUPPLIERS_HEADERS.",
    "Key-disjointness of shopify_orderapp vs weeklySummarize-derived online rows is real and pinned by test case5; cleanupOnlineRevenueSummaryRows correctly skips pull-owned rows.",
    "Nested withScriptLock_ (greenBeanPull -> weeklySummarize) is safe — SCRIPT_LOCK_DEPTH_ makes it reentrant with a single acquire/release.",
    "isoWeekLabel_ implements the ISO Thursday rule correctly and is DST-safe (Math.round over local-midnight dates); greenBeanWindow_ is pure string arithmetic.",
    "ARCHIVE_RETENTION_DAYS=183 exceeds the ~90-day greenBean window, so archiveAndPurge_ cannot purge rows the next pull would re-append as 'new'.",
    "Secrets: ORDER_APP_COST_TOKEN is read from Script Properties only, never logged; the token-bearing URL is never logged; only response bodies and the non-secret /exec URL reach logs and Calendar descriptions.",
    "No orphaned references to the deleted shopify.gs; per TODO.md it was never activated in production, so no live trigger is left pointing at a missing handler.",
    "Deleted-invoice staleness is a documented limitation with a runbook (docs/schema.md), not an oversight.",
    "Architecture: GAS-native pulls writing through ingestSupplierRows/upsertRows_ are the sanctioned exception per CLAUDE.md; docs/architecture.md, api.md, schema.md and ingest-contract.md were all updated to match the code."
  ]
}
