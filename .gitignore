node_modules/

# Share team-facing Claude config (settings, hooks, commands); ignore everything
# else under .claude/ (memory, personal state, credentials).
.claude/*
!.claude/settings.json
!.claude/hooks/
!.claude/commands/

.env
.env.local
credentials/
*.credentials.json
secrets/

# Downloaded business data — never commit PII or financials
downloads/
*.csv
*.xlsx
*.pdf

# Playwright saved sessions (contain auth cookies)
sessions/

# Harness phase execution outputs (contain stdout/stderr dumps)
phases/**/step*-output.json
phases/**/phase*-output.json

# Local operational bootstrap script — swept into a phase branch by the backup
# daemon (handles a Windows account password via Get-Credential). Ignored so it
# stops auto-committing; delete this line to track it deliberately on its own commit.
scripts/setup_always_on.ps1

# clasp config (transient, per-env)
.clasp.json

# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/
logs/
