# Copy to .env and fill in. .env is gitignored — never commit real values.
# Better: leave password blank and type it into the browser at the prompt.

BRIGHTHR_URL=https://app.brighthr.com.au
BRIGHTHR_USERNAME=
BRIGHTHR_PASSWORD=
